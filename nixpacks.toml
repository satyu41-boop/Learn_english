# Railway Nixpacks Configuration

[phases.setup]
nixPkgs = ["python311", "ffmpeg", "gcc", "musl-dev"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app"
